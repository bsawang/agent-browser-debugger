# agent-browser-debugger

Chrome 前端接管调试（CDP）——实时查看/调试**任意浏览器应用**的前端状态（DOM/布局/渲染/预览、任何「前端实际长什么样」的疑问）。ComfyUI 是常见调试对象之一，专题命令见 `docs/comfy/commands.md`。

## 项目定位

- **观察型调试**：接管用户已运行的 Chrome（DevTools 协议），看真实前端状态；不黑盒、不自行启动/重启浏览器
- **产物**：`skills/cdp-observe/`（可用的 skill）+ `docs/`（配置与常用命令）

## 模式（⚠️ 硬性要求：一律走常驻代理长连接）

调试一次少则 4~5 条命令，单次命令每条都是新连接、各弹一次授权；**即使再小的调试也只用常驻代理**。

| 模式 | 命令 | 授权弹窗 | 用途 |
|---|---|---|---|
| **常驻（唯一方式）** | 后台 `python skills/cdp-observe/scripts/cdp_proxy.py 9333`，然后 `curl http://127.0.0.1:9333/ -d '{"eval":"..."}'` 发命令 | 启动时一次 | 所有调试 |
| 单次（仅兜底） | `python skills/cdp-observe/scripts/cdp.py list\|eval\|multi\|shot` | 每次连接弹一次 | 代理不可用/无法重起时；优先 `multi` |

## 前置

用户 Chrome 需开启远程调试，二选一：
- **chrome://inspect/#remote-debugging** 勾选「Allow remote debugging for this browser instance」——默认 profile 保留登录，但每次**新连接**弹授权（Chrome 144+ 无永久信任）
- 或专用 profile + `--remote-debugging-port`（传统模式无授权，但独立 profile 需重配登录）

连接方式自动探测：先读 `DevToolsActivePort`（inspect 模式），回退传统 HTTP。

## 约定 ⚠️

- **只观察 + 临时调试**：不保存页面/应用状态、不改浏览器设置/配置/书签/扩展
- **不自行启动/重启浏览器**：端口没开时提示用户操作（桌面「Chrome（Claude可接管）」快捷方式）
- 临时 JS 注入刷新即还原；结束不留痕
- 需改持久内容（保存应用状态等）先征得用户同意

## 命令归档规约 ⚠️

**命令与踩坑按软件/主题归档到固定文档**，新条目定案后**追加**（不覆盖既有条目，文档是常用命令的权威源）：

| 命令类型 | 归档位置 |
|---|---|
| 调试本身（Chrome/CDP 通用：cdp.py、代理、端口检查、Chrome 配置） | `docs/commands.md` |
| **ComfyUI 专题**（观察 JS、触发执行、重启、节点/预览踩坑） | `docs/comfy/commands.md` |
| Chrome 远程调试原理与配置 | `docs/setup.md` |

- SKILL.md 只描述**触发场景/工作流/约定**，具体命令一律指到上述文档
- 每次调试遇到**新的可复用命令或坑**，定案后立即补进对应文档（一句话命令 + 用途即可），保持可查
- 修改文档后同步 README 的索引（如结构变化）

## 文档

- [docs/commands.md](docs/commands.md) — 调试命令（CDP 工具、Chrome 配置）
- [docs/comfy/commands.md](docs/comfy/commands.md) — **ComfyUI 专题命令**（观察节点/触发执行/重启/踩坑）
- [docs/setup.md](docs/setup.md) — Chrome 远程调试配置
